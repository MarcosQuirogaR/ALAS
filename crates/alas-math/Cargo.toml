[package]
name = "alas-math"
description = "Numerical primitives with no single Python module of their own: pseudospectral operators, splines and small dense linear algebra."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

[dependencies]
thiserror = { workspace = true }
# The dense LU behind `linalg`: see the workspace manifest for why a
# LAPACK-class kernel replaced the scalar elimination that lived here.
faer = { workspace = true }

[dev-dependencies]
alas-testkit = { path = "../alas-testkit" }
serde = { workspace = true }

[lints]
workspace = true

// TEMPORARY STUB written during a repository audit so the workspace manifest
// loads: this crate was left mid-write with no lib.rs, which broke
// `cargo test --workspace` for every crate. The real module declarations for
// layout.rs and numeric.rs belong to whoever resumes the alas-payload port.
// Delete this file when resuming.

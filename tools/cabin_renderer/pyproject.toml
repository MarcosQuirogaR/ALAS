[project]
name = "alas-cabin-renderer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["shapely==2.1.1", "CairoSVG==2.8.2"]

[project.scripts]
alas-cabin-render = "cabin_renderer.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
